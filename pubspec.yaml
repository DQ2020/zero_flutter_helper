name: zero_flutter_helper
description: base widget
version: 0.1.1
author: 'DanQi'
homepage: 'https://github.com/DQ2020/zero_flutter_helper.git'
update_note: ''
publish_to: http://10.67.9.31:6453

environment:
  sdk: ">=2.16.1 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_web_plugins:
    sdk: flutter

  styled_widget: 0.4.0+3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter.
flutter:
  uses-material-design: true
  plugin:
    implements: zero_flutter_helper
    platforms:
      android:
        package: com.kd.zero.android
        pluginClass: HelloPlugin

  assets:
    - image/
